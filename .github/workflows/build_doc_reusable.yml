name: Build Doc Reusable

on:
  workflow_call:
    inputs:
      lib:
        description: "Library folder name"
        required: true
        type: string
      doc_path:
        description: "Path to documentation source relative to library folder"
        required: true
        type: string
      artifact_name:
        description: "Name of the artifact to upload"
        required: true
        type: string
      use_container:
        description: "Whether to use the semantic_world container"
        required: false
        type: boolean
        default: false
      repo_lowercase:
        description: "(optional) fully qualified lowercase container image (e.g. ghcr.io/owner/repo:jazzy). If provided it will be used for container.image"
        required: false
        type: string


jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ${{ inputs.use_container && inputs.repo_lowercase || '' }}
    defaults:
      run:
        shell: ${{ inputs.use_container && 'bash -ieo pipefail {0}' || 'bash' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: '.'
          submodules: 'true'

      - name: Initialize submodules explicitly
        run: |
          git config --global --add safe.directory "*"
          git submodule sync --recursive
          git submodule update --init --recursive

      - name: Set up Python (Non-container)
        if: ${{ !inputs.use_container }}
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install Node.js (Container only)
        if: ${{ inputs.use_container }}
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Graphviz
        if: ${{ inputs.lib == 'coraplex' || inputs.lib == 'probabilistic_model' || inputs.lib == 'random_events' || inputs.lib == 'krrood' || inputs.lib == 'semantic_digital_twin' }}
        run: |
          sudo apt-get update
          sudo apt-get install graphviz libgraphviz-dev -y

      - name: Install dependencies (Non-container)
        if: ${{ !inputs.use_container }}
        run: |
          pip install -U pip uv
          # uv resolves the workspace via [tool.uv.sources], installing every
          # member editable from local paths; the doc tooling comes from the
          # root project's [project.optional-dependencies] "doc" extra.
          uv sync --extra doc
          uv pip install pybind11 pygraphviz

      - name: Install dependencies (Container)
        if: ${{ inputs.use_container }}
        run: |
          sudo apt-get update
          source /opt/ros/cram-env/bin/activate
          pip install -U pip uv
          uv sync --extra doc --active --inexact
          uv pip install pygraphviz

      # Leaves only the example solutions in the published pages. Runs on the exercise
      # sources themselves rather than through the doc/ symlink, because the book root
      # differs per library (doc for most, doc/source for coraplex).
      - name: Strip exercise-tagged cells
        if: ${{ hashFiles(format('{0}/self_assessment/exercises/*.md', inputs.lib)) != '' }}
        run: |
          if [ "${{ inputs.use_container }}" = "true" ]; then
            source /opt/ros/cram-env/bin/activate
            run_tool=""
          else
            # The doc tooling comes from the root project's "doc" extra, so resolve the
            # environment there rather than from the library the exercises live in.
            run_tool="uv run --no-sync --project $GITHUB_WORKSPACE"
          fi
          cd $GITHUB_WORKSPACE/${{ inputs.lib }}/self_assessment/exercises
          find . -type f -name '*.md' -print0 | while IFS= read -r -d '' f; do
            ipynb="${f%.md}.ipynb"
            $run_tool jupytext --from md:myst --to ipynb "$f" -o "$ipynb"
            $run_tool jupyter nbconvert --config $GITHUB_WORKSPACE/${{ inputs.lib }}/scripts/configs/nb_remove_exercises.json \
              --to notebook --inplace \
              "$ipynb"
            $run_tool jupytext --to md:myst "$ipynb" -o "$f"
            rm -f "$ipynb"
          done

      - name: Build the book (Non-container)
        if: ${{ !inputs.use_container }}
        run: |
          # --no-sync so uv does not re-resolve and drop the pip-installed pygraphviz
          uv run --no-sync jupyter-book build ${{ inputs.lib }}/${{ inputs.doc_path }}

      - name: Build the book (Container)
        if: ${{ inputs.use_container }}
        run: |
          source /opt/ros/overlay_ws/install/setup.bash
          source /opt/ros/cram-env/bin/activate
          cd $GITHUB_WORKSPACE/${{ inputs.lib }}
          jupyter-book build ${{ inputs.doc_path }}

      - name: Upload artifact (Non-container)
        if: ${{ !inputs.use_container && !env.ACT }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact_name }}
          path: "${{ inputs.lib }}/${{ inputs.doc_path }}/_build/html"

      - name: Upload artifact (Container)
        if: ${{ inputs.use_container && (github.event_name == 'push' || github.event_name == 'workflow_call' || github.event_name == 'workflow_dispatch') }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact_name }}
          path: "${{ inputs.lib }}/${{ inputs.doc_path }}/_build/html"
