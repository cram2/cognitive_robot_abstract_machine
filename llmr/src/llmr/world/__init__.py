"""World state access via SymbolGraph — no world-package or world-object imports.

  serializer — converts SymbolGraph contents to an LLM-readable world-context string.
  grounder   — resolves LLM-produced EntityDescriptionSchema to live Symbol instances.
"""
